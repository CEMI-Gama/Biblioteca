<h1>Todos os livros</h1>

<table border='2px' style='width': 50%>
	<tr>
		<td>Título</td>
		<td>Autor</td>
		<td>Ano</td>
		<td>Quantidade</td>
	</tr>
<% @books.each do |book| %>
<tr>
		<td><%= link_to book.title, books_seebook_path(book.id) %></td>
		<td><%= link_to book.author, books_seebook_path(book.id)%></td>
        <td><%= link_to book.year, books_seebook_path(book.id)%></td>
        <td><%= link_to book.amount, books_seebook_path(book.id)%></td>

</tr>
<% end %>
</table>